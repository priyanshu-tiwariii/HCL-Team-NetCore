import { MyOrdersComponent } from './my-orders.component';

describe('MyOrdersComponent', () => {
  it('should create an instance', () => {
    expect(new MyOrdersComponent()).toBeTruthy();
  });
});
